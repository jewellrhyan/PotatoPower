import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Workout Generator (Lazy Edition)',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
